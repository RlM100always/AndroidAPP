<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/baseline_share_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/baseline_share_24"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActivityLifeCycleAndIntent"
        tools:targetApi="31">
        <activity
            android:name=".Explicit1"
            android:exported="true">
        </activity>

        <activity
            android:name=".AleartDialogue"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity
            android:name=".Explicit2"
            android:exported="false" />
        <activity
            android:name=".Mxplicit1"
            android:exported="true">

        </activity>
        <activity
            android:name=".Intentgo"
            android:exported="true">

        </activity>
    </application>

</manifest>